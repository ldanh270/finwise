{
	"version": 2,
	"builds": [
		{
			"src": "frontend/package.json",
			"use": "@vercel/next"
		},
		{
			"src": "backend/src/main.ts",
			"use": "@vercel/node"
		}
	],
	"installCommand": "npm --prefix backend --ignore-scripts install --no-package-lock --no-audit --no-fund && npm --prefix frontend --ignore-scripts install --no-package-lock --no-audit --no-fund",
	"rewrites": [
		{
			"source": "/v1/(.*)",
			"destination": "/backend/src/main.ts"
		},
		{
			"source": "/(.*)",
			"destination": "/frontend/$1"
		}
	]
}
